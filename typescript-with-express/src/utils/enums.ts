export enum REQUEST_TYPE {
  GET = "get",
  POST = "post",
  DELETE = "delete",
  PATCH = "patch",
  PUT = "put",
}

export enum METADATA_KEYS {
  METHOD = "method",
  PATH = "path",
  MIDDLEWARE = "middleware",
  VALIDATOR = "validator",
}
